using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraBehavior : MonoBehaviour {

	// Use this for initialization
	void Start () {
        /*GameObject[] cameras = GameObject.FindGameObjectsWithTag("Camera");

        foreach (GameObject c in cameras)
        {
            Camera camera = c.GetComponent<Camera>();

            if (camera != null)
            {
                Transform camera_t = camera.GetComponent<Transform>();


                camera_t.transform.position.Set(camera_t.transform.position.x, camera_t.transform.position.y, camera_t.transform.position.z);
            }
        }*/
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    
}
